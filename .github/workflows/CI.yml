name: CI

on:
  
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:
  
jobs:

  build:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v3

      - name: Rodar Ola mundo!

        run: echo Ola, mundo!

      - name: Rodar outra ação
        run: echo outras ação para o build